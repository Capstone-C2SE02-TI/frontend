import { createChart, ColorType } from 'lightweight-charts';
import React, { useEffect, useRef } from 'react';

const ChartComponent = () => {


  return <div>fck u</div>;
};

export default ChartComponent;
